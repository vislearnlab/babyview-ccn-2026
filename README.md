# Beyond Curated Datasets — CCN 2026 Satellite Event

Static site for the satellite event at [CCN 2026](https://2026.ccneuro.org/) on **Sunday, August 2, 2026** at **New York University**.

**Live site:** _set after enabling GitHub Pages_

## Editing

Plain HTML + CSS. No build step.

- `index.html` — page content (talks, schedule, organizers)
- `styles.css` — styling

Open `index.html` directly in a browser to preview locally.

## Before launch

Two `href="#"` placeholders need the Google Form URL:
1. Hero CTA — `id="register-cta"`
2. Attending section callout — `id="register-link"`
